# This file is intentionally left empty.
# It is a workaround for a bug in the `adk web` command which incorrectly
# tries to load a YAML configuration. The presence of this empty file
# should allow the agent loader to fail gracefully and load the agent
# from main.py correctly.
